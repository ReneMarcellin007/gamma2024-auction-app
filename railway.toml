[build]
builder = "nixpacks"

[deploy]
startCommand = "dotnet Gamma2024.Server.dll"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[environments.production.variables]
ASPNETCORE_ENVIRONMENT = "Production"
PORT = "8080"